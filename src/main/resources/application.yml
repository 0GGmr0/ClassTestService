spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://10.10.0.145:3306/classtest?characterEncoding=utf-8&useSSL=false


server:
  servlet:
    context-path: /api
  port: 8081

mybatis:
  mapper-locations: classpath:mapping/*.xml
  type-aliases-package: com.gmr.vote.model.entity